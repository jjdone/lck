server:
  port: 8081

spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/h2db/lck
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        show_sql: true
        format_sql: true

logging:
  level:
    org.hibernate.sql: debug